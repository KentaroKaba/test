program main
    implicit none
    
    print *, "HELLO GitHub"
    print *, "HELLO New branch"

    ! add -> commit -> push 
    ! git add <filename>
    ! git commit　(-m "<comment>"でコメント追加　vimはCtrl+Iでinsert mode Ctrl+Cで戻って:wqで保存)
    ! git push -u origin main (2回目以降はgit pushのみ)
end program main